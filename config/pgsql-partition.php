<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Year partition database name prefix
    |--------------------------------------------------------------------------
    |
    | Resolved database name is {schema_prefix}{year} (e.g. qualisys_2026).
    | Leave null to use YearConnection's default (qualisys_), unless you pass
    | an explicit prefix to YearConnection::connection().
    |
    */

    'schema_prefix' => null,

];
